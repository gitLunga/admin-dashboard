import React, {useState, useEffect, useCallback} from 'react';import {    Box, Paper, Typography, Grid, Chip, Alert, CircularProgress,    Button, Divider, Avatar, IconButton, Dialog, DialogTitle,    DialogContent, DialogActions, TextField,} from '@mui/material';import {    Email as EmailIcon, Phone as PhoneIcon, LocationOn as LocationIcon,    Work as WorkIcon, CalendarToday as CalendarIcon,    Edit as EditIcon, ArrowBack as ArrowBackIcon,    CheckCircle as VerifiedIcon, Pending as PendingIcon, Block as RejectedIcon,    PictureAsPdf as PdfIcon, Description, Image as ImageIcon, Close as CloseIcon,    Shield as ShieldIcon,} from '@mui/icons-material';import {useParams, useNavigate, useLocation} from 'react-router-dom';import {adminAPI} from '../../services/api';import {format} from 'date-fns';import StatusUpdateModal from './StatusUpdateModal';import InvoiceViewer from '../admin/InvoiceViewer';import QuickDocumentActions from '../../components/admin/QuickDocumentActions';const T = {    bg: '#F8F9FC', surface: '#FFFFFF', border: '#E8ECF4',    text: '#0F1F3D', muted: '#6B7A99',    accent: '#1E4FD8', accentSoft: '#EBF0FF',    green: '#059669', greenSoft: '#D1FAE5',    amber: '#D97706', amberSoft: '#FEF3C7',    rose: '#DC2626', roseSoft: '#FEE2E2',    purple: '#7C3AED', purpleSoft: '#EDE9FE',};const STATUS_META = {    Verified: {color: T.green, soft: T.greenSoft, Icon: VerifiedIcon},    Pending:  {color: T.amber, soft: T.amberSoft, Icon: PendingIcon},    Rejected: {color: T.rose,  soft: T.roseSoft,  Icon: RejectedIcon},};const StatusBadge = ({status}) => {    const m = STATUS_META[status] || {color: T.muted, soft: '#F1F5F9'};    return (        <Box sx={{            display: 'inline-flex', alignItems: 'center', gap: 0.7,            px: 1.2, py: 0.4, borderRadius: '20px',            bgcolor: m.soft, border: `1px solid ${m.color}28`        }}>            <Box sx={{width: 6, height: 6, borderRadius: '50%', bgcolor: m.color}}/>            <Typography sx={{fontSize: '0.72rem', fontWeight: 600, color: m.color}}>{status}</Typography>        </Box>    );};const InfoItem = ({icon: Icon, label, value}) => (    <Box sx={{        display: 'flex', alignItems: 'flex-start', gap: 1.2,        py: 1.3, borderBottom: `1px solid ${T.border}`,        '&:last-child': {borderBottom: 'none'}    }}>        <Box sx={{            width: 28, height: 28, borderRadius: '8px', bgcolor: T.bg,            display: 'flex', alignItems: 'center', justifyContent: 'center',            flexShrink: 0, mt: 0.1        }}>            <Icon sx={{fontSize: 14, color: T.muted}}/>        </Box>        <Box>            <Typography sx={{                fontSize: '0.67rem', fontWeight: 700, color: T.muted,                textTransform: 'uppercase', letterSpacing: 0.7            }}>{label}</Typography>            <Typography sx={{fontSize: '0.83rem', color: T.text, fontWeight: 500}}>{value || '—'}</Typography>        </Box>    </Box>);const TabBtn = ({label, active, onClick}) => (    <Button onClick={onClick} sx={{        borderRadius: '8px', textTransform: 'none', fontFamily: 'Plus Jakarta Sans, sans-serif',        fontWeight: active ? 700 : 500, fontSize: '0.83rem', px: 2, py: 0.9,        color: active ? T.accent : T.muted, bgcolor: active ? T.accentSoft : 'transparent',        position: 'relative', '&:hover': {bgcolor: active ? T.accentSoft : T.bg},        transition: 'all 0.15s ease',    }}>        {label}        {active && (            <Box sx={{                position: 'absolute', bottom: 0, left: '50%',                transform: 'translateX(-50%)',                width: 20, height: 2, bgcolor: T.accent, borderRadius: 2            }}/>        )}    </Button>);const OverviewField = ({label, value}) => (    <Box sx={{p: 2, borderRadius: '10px', bgcolor: T.bg, border: `1px solid ${T.border}`}}>        <Typography sx={{            fontSize: '0.67rem', fontWeight: 700, color: T.muted,            textTransform: 'uppercase', letterSpacing: 0.7, mb: 0.5        }}>{label}</Typography>        <Typography sx={{fontSize: '0.88rem', fontWeight: 600, color: T.text}}>{value || '—'}</Typography>    </Box>);const fmtDate = (d) => {    try { return format(new Date(d), 'PPP'); }    catch { return '—'; }};const UserDetail = () => {    const {id}       = useParams();    const navigate   = useNavigate();    const location   = useLocation();    // ─── Determine user type from URL path ────────────────────────────────────    // /operational-users/:id  → fetch operational directly    // /client-users/:id or /users/:id → fetch client directly    const isOperationalRoute = location.pathname.includes('operational-users');    const [user, setUser]                   = useState(null);    const [loading, setLoading]             = useState(true);    const [error, setError]                 = useState(null);    const [activeTab, setActiveTab]         = useState(0);    const [modalOpen, setModalOpen]         = useState(false);    const [invoiceDialogOpen, setInvoiceDialogOpen] = useState(false);    const [userDocuments, setUserDocuments] = useState([]);    const [loadingDocs, setLoadingDocs]     = useState(false);    const [docsError, setDocsError]         = useState(null);    const [selectedDocument, setSelectedDocument] = useState(null);    const [statusDialogOpen, setStatusDialogOpen] = useState(false);    const [statusValue, setStatusValue]     = useState('Pending');    const [statusNotes, setStatusNotes]     = useState('');    const isClientUser = user?.user_type === 'client';    useEffect(() => {        if (selectedDocument) {            setStatusValue(selectedDocument.document_status);            setStatusNotes('');        }    }, [selectedDocument]);    // ─── Fetch user — no more guessing, use route to decide ──────────────────    const fetchUserDetails = useCallback(async () => {        try {            setLoading(true);            setError(null);            if (isOperationalRoute) {                // ✅ Operational route — go directly, never touch client table                const r = await adminAPI.getOperationalUserById(id);                setUser({...r.data.data.user, user_type: 'operational'});            } else {                // ✅ Client route — go directly to client table                try {                    const r = await adminAPI.getClientUserById(id);                    setUser({...r.data.data.user, user_type: 'client'});                } catch {                    // Only fall back if this is a generic /users route and client not found                    const r = await adminAPI.getOperationalUserById(id);                    setUser({...r.data.data.user, user_type: 'operational'});                }            }        } catch {            setError('User not found');        } finally {            setLoading(false);        }    }, [id, isOperationalRoute]);    const fetchUserDocuments = useCallback(async () => {        if (!isClientUser) return;        try {            setLoadingDocs(true);            setDocsError(null);            const r = await adminAPI.getUserDocuments(id);            setUserDocuments(r.data.data.documents || []);        } catch {            setDocsError('Failed to load documents');        } finally {            setLoadingDocs(false);        }    }, [id, isClientUser]);    const handleDocumentStatusUpdate = async (status, notes) => {        if (!selectedDocument) return;        try {            await adminAPI.updateDocumentStatus(selectedDocument.document_id, status, notes || '');            setStatusDialogOpen(false);            setSelectedDocument(null);            fetchUserDocuments();        } catch {            setDocsError('Failed to update document status');        }    };    const handleStatusUpdate = async (status, notes) => {        try {            await adminAPI.updateUserStatus(id, {status, notes});            fetchUserDetails();            setModalOpen(false);        } catch (err) {            setError(err.message || 'Failed to update status');        }    };    useEffect(() => {fetchUserDetails();}, [fetchUserDetails]);    useEffect(() => {        if (isClientUser && activeTab === 2) fetchUserDocuments();    }, [activeTab, isClientUser, fetchUserDocuments]);    // ─── Tabs differ by user type ─────────────────────────────────────────────    const TABS = isOperationalRoute        ? ['Overview', 'Activity']        : ['Overview', 'Activity', 'Documents', 'Contract'];    // ─── Loading / error states ───────────────────────────────────────────────    if (loading) return (        <Box sx={{display: 'flex', justifyContent: 'center', alignItems: 'center', minHeight: 400, bgcolor: T.bg}}>            <CircularProgress sx={{color: T.accent}}/>        </Box>    );    if (error || !user) return (        <Box sx={{p: 3, bgcolor: T.bg, minHeight: '100vh'}}>            <Alert severity="error" sx={{borderRadius: '10px'}}>{error || 'User not found'}</Alert>        </Box>    );    return (        <Box sx={{p: {xs: 2, md: 3.5}, bgcolor: T.bg, minHeight: '100vh'}}>            {/* ── Header ─────────────────────────────────────────────────────── */}            <Box sx={{display: 'flex', alignItems: 'center', gap: 1.5, mb: 3, animation: 'fadeUp 0.4s ease-out'}}>                <IconButton onClick={() => navigate(-1)} size="small"                            sx={{                                borderRadius: '10px', border: `1px solid ${T.border}`,                                bgcolor: T.surface, color: T.muted,                                '&:hover': {bgcolor: T.accentSoft, color: T.accent, borderColor: T.accent}                            }}>                    <ArrowBackIcon sx={{fontSize: 18}}/>                </IconButton>                <Box>                    <Typography sx={{                        fontSize: {xs: '1.2rem', md: '1.55rem'},                        fontWeight: 800, color: T.text, letterSpacing: '-0.3px'                    }}>                        User Details                    </Typography>                    <Typography sx={{fontSize: '0.75rem', color: T.muted}}>                        {isOperationalRoute ? 'Operational User' : 'Client User'} · #{id}                    </Typography>                </Box>            </Box>            <Grid container spacing={2.5}>                {/* ── Profile Card ────────────────────────────────────────────── */}                <Grid item xs={12} md={4}>                    <Paper elevation={0} sx={{                        borderRadius: '14px', border: `1px solid ${T.border}`,                        bgcolor: T.surface, overflow: 'hidden',                        animation: 'fadeUp 0.45s ease-out 0.05s both'                    }}>                        <Box sx={{height: 5, bgcolor: isOperationalRoute ? T.purple : T.accent}}/>                        <Box sx={{p: 3}}>                            <Box sx={{display: 'flex', flexDirection: 'column', alignItems: 'center', mb: 2.5}}>                                <Avatar sx={{                                    width: 72, height: 72, mb: 1.5, borderRadius: '18px',                                    bgcolor: isOperationalRoute ? T.purpleSoft : T.accentSoft,                                    color:   isOperationalRoute ? T.purple     : T.accent,                                    fontSize: '1.4rem', fontWeight: 700,                                    fontFamily: 'Plus Jakarta Sans, sans-serif'                                }}>                                    {user.first_name?.[0]}{user.last_name?.[0]}                                </Avatar>                                <Typography sx={{                                    fontWeight: 800, fontSize: '1rem', color: T.text,                                    textAlign: 'center', mb: 0.8                                }}>                                    {user.title && (                                        <span style={{color: T.muted, fontWeight: 500, marginRight: 4, fontSize: '0.9rem'}}>                                            {user.title}.                                        </span>                                    )}                                    {user.first_name} {user.last_name}                                </Typography>                                <Box sx={{display: 'flex', gap: 1, flexWrap: 'wrap', justifyContent: 'center'}}>                                    <Chip                                        label={isOperationalRoute ? 'Operational User' : 'Client User'}                                        size="small"                                        sx={{                                            height: 22, fontSize: '0.7rem', fontWeight: 600,                                            bgcolor: isOperationalRoute ? T.purpleSoft : T.accentSoft,                                            color:   isOperationalRoute ? T.purple     : T.accent                                        }}/>                                    {/* Super admin badge for operational users */}                                    {isOperationalRoute && user.is_super_admin && (                                        <Chip                                            icon={<ShieldIcon sx={{fontSize: '10px !important', color: `${T.green} !important`}}/>}                                            label="Super Admin"                                            size="small"                                            sx={{                                                height: 22, fontSize: '0.7rem', fontWeight: 600,                                                bgcolor: T.greenSoft, color: T.green,                                                '& .MuiChip-icon': {ml: '4px'}                                            }}/>                                    )}                                    {/* Registration status badge for client users */}                                    {!isOperationalRoute && user.registration_status && (                                        <StatusBadge status={user.registration_status}/>                                    )}                                </Box>                            </Box>                            <Divider sx={{borderColor: T.border, mb: 2}}/>                            {/* ── Contact info ──────────────────────────────── */}                            <InfoItem icon={EmailIcon} label="Email" value={user.email}/>                            {user.phone_number && (                                <InfoItem icon={PhoneIcon} label="Phone" value={user.phone_number}/>                            )}                            {user.region && (                                <InfoItem icon={LocationIcon} label="Region" value={user.region}/>                            )}                            {/* ── Role-specific fields ──────────────────────── */}                            {isOperationalRoute ? (                                <>                                    <InfoItem icon={WorkIcon} label="Role" value={user.user_role}/>                                    <InfoItem                                        icon={ShieldIcon}                                        label="Access Level"                                        value={user.is_super_admin ? 'Super Admin' : 'Standard'}                                    />                                </>                            ) : (                                <>                                    {user.persal_id && (                                        <InfoItem icon={WorkIcon} label="Persal ID" value={user.persal_id}/>                                    )}                                    {user.department_id && (                                        <InfoItem icon={WorkIcon} label="Department" value={user.department_id}/>                                    )}                                </>                            )}                            {user.created_at && (                                <InfoItem icon={CalendarIcon} label="Registered" value={fmtDate(user.created_at)}/>                            )}                            {/* ── Update Status button — client users only ──── */}                            {!isOperationalRoute && (                                <Box sx={{mt: 2.5}}>                                    <Button fullWidth variant="contained"                                            startIcon={<EditIcon sx={{fontSize: '16px !important'}}/>}                                            onClick={() => setModalOpen(true)}                                            sx={{                                                borderRadius: '10px', textTransform: 'none', fontWeight: 700,                                                fontFamily: 'Plus Jakarta Sans, sans-serif', fontSize: '0.83rem',                                                bgcolor: T.accent, boxShadow: 'none',                                                '&:hover': {bgcolor: '#1641B8', boxShadow: `0 4px 14px ${T.accent}44`}                                            }}>                                        Update Status                                    </Button>                                </Box>                            )}                        </Box>                    </Paper>                </Grid>                {/* ── Tabs + Content ──────────────────────────────────────────── */}                <Grid item xs={12} md={8}>                    {/* Tab bar */}                    <Paper elevation={0} sx={{                        borderRadius: '14px', border: `1px solid ${T.border}`,                        bgcolor: T.surface, p: 0.8, mb: 2,                        display: 'flex', gap: 0.5, flexWrap: 'wrap',                        animation: 'fadeUp 0.45s ease-out 0.1s both'                    }}>                        {TABS.map((tab, i) => (                            <TabBtn key={tab} label={tab} active={activeTab === i}                                    onClick={() => setActiveTab(i)}/>                        ))}                    </Paper>                    {/* ── Tab 0: Overview ──────────────────────────────────────── */}                    {activeTab === 0 && (                        <Paper elevation={0} sx={{                            borderRadius: '14px', border: `1px solid ${T.border}`,                            bgcolor: T.surface, p: 3, animation: 'fadeUp 0.4s ease-out'                        }}>                            <Typography sx={{fontWeight: 700, fontSize: '0.9rem', color: T.text, mb: 2}}>                                User Information                            </Typography>                            {isOperationalRoute ? (                                // ── Operational user overview ──────────────────                                <Grid container spacing={1.5}>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="First Name"  value={user.first_name}/>                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="Last Name"   value={user.last_name}/>                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="Email"       value={user.email}/>                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="Role"        value={user.user_role}/>                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="Title"       value={user.title || 'Not set'}/>                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField                                            label="Access Level"                                            value={user.is_super_admin ? 'Super Admin' : 'Standard'}                                        />                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField                                            label="Member Since"                                            value={user.created_at ? fmtDate(user.created_at) : null}                                        />                                    </Grid>                                    <Grid item xs={12} sm={6}>                                        <OverviewField label="User ID" value={`#${user.op_user_id}`}/>                                    </Grid>                                </Grid>                            ) : (                                // ── Client user overview ──────────────────────                                <Grid container spacing={1.5}>                                    {[                                        {label: 'User Type',          value: user.user_type},                                        {label: 'Network Provider',   value: user.network_provider},                                        {label: 'Contract Duration',  value: user.contract_duration_months ? `${user.contract_duration_months} months` : null},                                        {label: 'Contract End Date',  value: user.contract_end_date ? fmtDate(user.contract_end_date) : null},                                    ].map(({label, value}) => (                                        <Grid item xs={12} sm={6} key={label}>                                            <OverviewField label={label} value={value}/>                                        </Grid>                                    ))}                                    {user.verification_notes && (                                        <Grid item xs={12}>                                            <Box sx={{                                                p: 2, borderRadius: '10px',                                                bgcolor: T.amberSoft, border: `1px solid ${T.amber}22`                                            }}>                                                <Typography sx={{                                                    fontSize: '0.68rem', fontWeight: 700, color: T.amber,                                                    textTransform: 'uppercase', letterSpacing: 0.7, mb: 0.8                                                }}>                                                    Verification Notes                                                </Typography>                                                <Typography sx={{fontSize: '0.83rem', color: T.text}}>                                                    {user.verification_notes}                                                </Typography>                                            </Box>                                        </Grid>                                    )}                                </Grid>                            )}                        </Paper>                    )}                    {/* ── Tab 1: Activity ──────────────────────────────────────── */}                    {activeTab === 1 && (                        <Paper elevation={0} sx={{                            borderRadius: '14px', border: `1px solid ${T.border}`,                            bgcolor: T.surface, p: 3, animation: 'fadeUp 0.4s ease-out'                        }}>                            <Typography sx={{fontWeight: 700, fontSize: '0.9rem', color: T.text, mb: 2}}>                                Recent Activity                            </Typography>                            <Box sx={{py: 5, textAlign: 'center'}}>                                <CalendarIcon sx={{fontSize: 44, color: T.border, mb: 1.5}}/>                                <Typography sx={{fontSize: '0.85rem', color: T.muted}}>                                    Activity history will be displayed here                                </Typography>                            </Box>                        </Paper>                    )}                    {/* ── Tab 2: Documents (client only) ───────────────────────── */}                    {activeTab === 2 && !isOperationalRoute && (                        <Paper elevation={0} sx={{                            borderRadius: '14px', border: `1px solid ${T.border}`,                            bgcolor: T.surface, p: 3, animation: 'fadeUp 0.4s ease-out'                        }}>                            <Box sx={{display: 'flex', justifyContent: 'space-between', alignItems: 'center', mb: 2}}>                                <Typography sx={{fontWeight: 700, fontSize: '0.9rem', color: T.text}}>                                    User Documents                                </Typography>                                {userDocuments.length > 0 && (                                    <Chip                                        label={`${userDocuments.length} doc${userDocuments.length !== 1 ? 's' : ''}`}                                        size="small"                                        sx={{                                            height: 22, fontSize: '0.7rem', fontWeight: 700,                                            bgcolor: T.accentSoft, color: T.accent,                                            fontFamily: 'JetBrains Mono, monospace'                                        }}/>                                )}                            </Box>                            {loadingDocs ? (                                <Box sx={{display: 'flex', justifyContent: 'center', py: 4}}>                                    <CircularProgress size={28} sx={{color: T.accent}}/>                                </Box>                            ) : docsError ? (                                <Alert severity="error" onClose={() => setDocsError(null)}                                       sx={{borderRadius: '10px'}}>{docsError}</Alert>                            ) : userDocuments.length === 0 ? (                                <Box sx={{py: 5, textAlign: 'center'}}>                                    <Description sx={{fontSize: 44, color: T.border, mb: 1.5}}/>                                    <Typography sx={{fontSize: '0.85rem', color: T.muted}}>                                        No documents uploaded yet                                    </Typography>                                </Box>                            ) : (                                <Box sx={{display: 'flex', flexDirection: 'column', gap: 1.2}}>                                    {userDocuments.map((doc) => {                                        const isPdf = doc.file_name?.toLowerCase().endsWith('.pdf');                                        const isImg = doc.file_name?.toLowerCase().match(/\.(jpg|jpeg|png)$/);                                        const m = STATUS_META[doc.document_status] || {                                            color: T.muted, soft: '#F1F5F9', Icon: PendingIcon                                        };                                        return (                                            <Box key={doc.document_id} sx={{                                                p: 2, borderRadius: '12px', bgcolor: T.bg,                                                border: `1px solid ${T.border}`,                                                display: 'flex', alignItems: 'flex-start', gap: 1.5,                                                flexWrap: {xs: 'wrap', sm: 'nowrap'},                                                transition: 'border-color 0.2s',                                                '&:hover': {borderColor: T.accent}                                            }}>                                                <Box sx={{                                                    width: 36, height: 36, borderRadius: '10px', flexShrink: 0,                                                    bgcolor: isPdf ? T.roseSoft : isImg ? T.accentSoft : T.bg,                                                    display: 'flex', alignItems: 'center', justifyContent: 'center',                                                    border: `1px solid ${T.border}`                                                }}>                                                    {isPdf                                                        ? <PdfIcon sx={{fontSize: 18, color: T.rose}}/>                                                        : isImg                                                            ? <ImageIcon sx={{fontSize: 18, color: T.accent}}/>                                                            : <Description sx={{fontSize: 18, color: T.muted}}/>}                                                </Box>                                                <Box sx={{flex: 1, minWidth: 0}}>                                                    <Box sx={{                                                        display: 'flex', alignItems: 'center',                                                        gap: 1, flexWrap: 'wrap', mb: 0.3                                                    }}>                                                        <Typography sx={{fontSize: '0.83rem', fontWeight: 700, color: T.text}}>                                                            {doc.document_type === 'Proof_of_Residence'                                                                ? 'Proof of Residence'                                                                : doc.document_type}                                                        </Typography>                                                        <Box sx={{                                                            display: 'inline-flex', alignItems: 'center', gap: 0.6,                                                            px: 1, py: 0.3, borderRadius: '20px',                                                            bgcolor: m.soft, border: `1px solid ${m.color}28`                                                        }}>                                                            <Box sx={{width: 5, height: 5, borderRadius: '50%', bgcolor: m.color}}/>                                                            <Typography sx={{fontSize: '0.67rem', fontWeight: 600, color: m.color}}>                                                                {doc.document_status}                                                            </Typography>                                                        </Box>                                                    </Box>                                                    <Typography sx={{fontSize: '0.72rem', color: T.muted}}>                                                        {doc.file_name}                                                    </Typography>                                                    <Typography sx={{fontSize: '0.7rem', color: T.muted}}>                                                        Uploaded: {fmtDate(doc.upload_date)}                                                        {doc.file_size && ` · ${(doc.file_size / 1024).toFixed(1)} KB`}                                                    </Typography>                                                    {doc.verification_notes && (                                                        <Box sx={{                                                            mt: 0.8, p: 1.2, borderRadius: '8px',                                                            bgcolor: T.amberSoft, border: `1px solid ${T.amber}22`                                                        }}>                                                            <Typography sx={{fontSize: '0.72rem', color: T.amber}}>                                                                Notes: {doc.verification_notes}                                                            </Typography>                                                        </Box>                                                    )}                                                </Box>                                                <Box sx={{display: 'flex', gap: 0.7, flexShrink: 0}}>                                                    <QuickDocumentActions                                                        documentId={doc.document_id}                                                        fileName={doc.file_name}                                                        documentType={doc.document_type}                                                        documentStatus={doc.document_status}                                                        isInvoice={doc.is_invoice}                                                        userId={id}                                                        userName={user ? `${user.first_name} ${user.last_name}` : ''}                                                    />                                                    <IconButton size="small" onClick={() => {                                                        setSelectedDocument(doc);                                                        setStatusDialogOpen(true);                                                    }} sx={{                                                        width: 30, height: 30, borderRadius: '8px',                                                        bgcolor: m.soft, color: m.color,                                                        border: `1px solid ${m.color}28`,                                                        '&:hover': {opacity: 0.82}                                                    }}>                                                        <m.Icon sx={{fontSize: 14}}/>                                                    </IconButton>                                                </Box>                                            </Box>                                        );                                    })}                                </Box>                            )}                        </Paper>                    )}                    {/* ── Tab 3: Contract (client only) ────────────────────────── */}                    {activeTab === 3 && !isOperationalRoute && (                        <Paper elevation={0} sx={{                            borderRadius: '14px', border: `1px solid ${T.border}`,                            bgcolor: T.surface, p: 3, animation: 'fadeUp 0.4s ease-out'                        }}>                            <Typography sx={{fontWeight: 700, fontSize: '0.9rem', color: T.text, mb: 2}}>                                Contract Details                            </Typography>                            <Grid container spacing={1.5}>                                <Grid item xs={12} sm={6}>                                    <OverviewField label="Contract Status" value="Active"/>                                </Grid>                                <Grid item xs={12} sm={6}>                                    <OverviewField                                        label="Last Renewal Date"                                        value={user.contract_end_date ? fmtDate(user.contract_end_date) : null}                                    />                                </Grid>                            </Grid>                        </Paper>                    )}                </Grid>            </Grid>            {/* ── Client-only modals ──────────────────────────────────────────── */}            {!isOperationalRoute && (                <>                    <StatusUpdateModal                        open={modalOpen} user={user}                        onClose={() => setModalOpen(false)}                        onSubmit={handleStatusUpdate}                    />                    <InvoiceViewer                        open={invoiceDialogOpen} userId={id}                        userName={`${user.first_name} ${user.last_name}`}                        onClose={() => setInvoiceDialogOpen(false)}                    />                </>            )}            {/* ── Document Status Dialog (client only) ────────────────────────── */}            <Dialog open={statusDialogOpen} onClose={() => setStatusDialogOpen(false)}                    maxWidth="sm" fullWidth                    PaperProps={{sx: {                            borderRadius: '16px', border: `1px solid ${T.border}`,                            boxShadow: '0 24px 60px rgba(15,31,61,0.14)', bgcolor: T.bg                        }}}>                <DialogTitle sx={{p: 0}}>                    <Box sx={{                        display: 'flex', justifyContent: 'space-between', alignItems: 'center',                        px: 3, py: 2.2, bgcolor: T.surface, borderBottom: `1px solid ${T.border}`                    }}>                        <Typography sx={{fontWeight: 700, fontSize: '0.95rem', color: T.text}}>                            Update Document Status                        </Typography>                        <IconButton size="small" onClick={() => setStatusDialogOpen(false)}                                    sx={{borderRadius: '8px', color: T.muted, '&:hover': {bgcolor: T.bg, color: T.rose}}}>                            <CloseIcon sx={{fontSize: 17}}/>                        </IconButton>                    </Box>                </DialogTitle>                <DialogContent sx={{p: 3, bgcolor: T.bg}}>                    {selectedDocument && (                        <Box>                            <Box sx={{                                p: 2, mb: 2.5, borderRadius: '12px',                                bgcolor: T.surface, border: `1px solid ${T.border}`                            }}>                                <Typography sx={{                                    fontSize: '0.68rem', fontWeight: 700, color: T.muted,                                    textTransform: 'uppercase', letterSpacing: 0.7, mb: 0.5                                }}>Document</Typography>                                <Typography sx={{fontSize: '0.87rem', fontWeight: 600, color: T.text, mb: 0.8}}>                                    {selectedDocument.document_type}                                </Typography>                                <Box sx={{display: 'flex', alignItems: 'center', gap: 1}}>                                    <Typography sx={{fontSize: '0.75rem', color: T.muted}}>Current:</Typography>                                    <StatusBadge status={selectedDocument.document_status}/>                                </Box>                            </Box>                            <Typography sx={{                                fontSize: '0.7rem', fontWeight: 700, color: T.muted,                                textTransform: 'uppercase', letterSpacing: 0.8, mb: 1                            }}>                                New Status                            </Typography>                            <Box sx={{display: 'flex', gap: 1, mb: 2.5, flexWrap: 'wrap'}}>                                {['Pending', 'Verified', 'Rejected'].map(s => {                                    const m = STATUS_META[s] || {color: T.muted, soft: '#F1F5F9'};                                    return (                                        <Box key={s} onClick={() => setStatusValue(s)} sx={{                                            flex: 1, minWidth: 80, p: 1.4, borderRadius: '10px',                                            cursor: 'pointer', textAlign: 'center',                                            border: `2px solid ${statusValue === s ? m.color : T.border}`,                                            bgcolor: statusValue === s ? m.soft : T.surface,                                            transition: 'all 0.15s ease',                                            '&:hover': {borderColor: m.color, bgcolor: m.soft}                                        }}>                                            <Box sx={{                                                width: 7, height: 7, borderRadius: '50%',                                                bgcolor: m.color, mx: 'auto', mb: 0.5                                            }}/>                                            <Typography sx={{                                                fontSize: '0.78rem', fontWeight: 700,                                                color: statusValue === s ? m.color : T.text                                            }}>{s}</Typography>                                        </Box>                                    );                                })}                            </Box>                            <Typography sx={{                                fontSize: '0.7rem', fontWeight: 700, color: T.muted,                                textTransform: 'uppercase', letterSpacing: 0.8, mb: 1                            }}>                                Notes {statusValue === 'Rejected' && <span style={{color: T.rose}}>*</span>}                            </Typography>                            <TextField                                fullWidth multiline rows={3}                                placeholder="Add notes about this verification…"                                value={statusNotes}                                onChange={e => setStatusNotes(e.target.value)}                                sx={{                                    '& .MuiOutlinedInput-root': {                                        borderRadius: '10px', bgcolor: T.surface, fontSize: '0.83rem',                                        '& fieldset': {borderColor: T.border},                                        '&:hover fieldset': {borderColor: T.accent},                                        '&.Mui-focused fieldset': {borderColor: T.accent}                                    },                                    '& textarea': {fontFamily: 'Plus Jakarta Sans, sans-serif'}                                }}                            />                        </Box>                    )}                </DialogContent>                <DialogActions sx={{                    px: 3, py: 2.2, bgcolor: T.surface,                    borderTop: `1px solid ${T.border}`, gap: 1.5                }}>                    <Button onClick={() => setStatusDialogOpen(false)} sx={{                        borderRadius: '10px', textTransform: 'none', fontWeight: 600,                        fontFamily: 'Plus Jakarta Sans, sans-serif', fontSize: '0.82rem',                        color: T.muted, border: `1px solid ${T.border}`,                        bgcolor: T.bg, '&:hover': {bgcolor: T.border}                    }}>                        Cancel                    </Button>                    <Button variant="contained"                            onClick={() => handleDocumentStatusUpdate(statusValue, statusNotes)}                            disabled={!selectedDocument || statusValue === selectedDocument?.document_status}                            sx={{                                borderRadius: '10px', textTransform: 'none', fontWeight: 700,                                fontFamily: 'Plus Jakarta Sans, sans-serif', fontSize: '0.82rem',                                bgcolor: T.accent, boxShadow: 'none',                                '&:hover': {bgcolor: '#1641B8', boxShadow: `0 4px 14px ${T.accent}44`},                                '&.Mui-disabled': {bgcolor: T.border, color: T.muted}                            }}>                        Update Status                    </Button>                </DialogActions>            </Dialog>        </Box>    );};export default UserDetail;